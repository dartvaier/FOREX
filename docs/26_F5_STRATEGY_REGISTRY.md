# F5.14 — Strategy & Hypothesis Registry

The JSON registry under `research/registry` is an audit layer only. Immutable hypotheses carry deterministic fingerprints, evidence references and parent lineage. Material changes require a new record. Validated lifecycle transitions and duplicate detection preserve historical evidence; the registry never runs Strategy, backtests, risk, execution or analytics.
